const activo = true;

// let mensaje = "";

// if (activo === true) {
//   mensaje = "Activo";
// } else {
//   mensaje = "Inactivo";
// }
const mensaje = activo >= 10 ? "Activo" : "Inactivo";
const mensaje = activo ? "Activo" : null;

const mensaje = activo && "Activo";

console.log(mensaje);
